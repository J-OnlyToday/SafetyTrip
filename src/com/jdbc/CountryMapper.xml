<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="CountryMapper">
	<insert id="countryInsert" parameterType="CountryDTO">
		insert into country(couno, cname, cename)
		values(SEQ_COUNTRY_couno.nextval, #{cname}, #{cename})
	</insert>
	<select id="countrySelectOneByCname" parameterType="string" resultType="CountryDTO">
		select couno, cname, cename, continent from country where cname = #{cname}
	</select>
	<select id="countrySelectListByCname" parameterType="hashmap" resultType="CountryDTO">
		select couno, cname, cename, continent from country where cname like '%#{cname}%'
		order by cname desc
		<if test="limit != 0">
			limit #{limit}
		</if>
	</select>
	<select id="countrySelectListByCename" parameterType="hashmap" resultType="CountryDTO">
		select couno, cname, cename, continent from country where cname like '%#{cename}%'
		order by cename desc
		<if test="limit != 0">
			limit #{limit}
		</if>
	</select>
</mapper>



